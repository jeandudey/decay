# Generated by decay. Do not edit.
#
# The options glib declares, defaulting to what meson itself would have
# chosen. Set them on a platform to configure this project. Options
# meson provides to every project live alongside, one directory up.

# build option `option:oss_fuzz`: Indicate oss-fuzz build environment
constraint(
    name = "oss_fuzz",
    values = ["enabled", "disabled", "auto"],
    default = "disabled",
    visibility = ["PUBLIC"],
)

# build option `option:libmount`: build with libmount support
constraint(
    name = "libmount",
    values = ["enabled", "disabled", "auto"],
    default = "auto",
    visibility = ["PUBLIC"],
)

# build option `option:selinux`: build with selinux support
constraint(
    name = "selinux",
    values = ["enabled", "disabled", "auto"],
    default = "auto",
    visibility = ["PUBLIC"],
)

# build option `option:sysprof`: include tracing support for sysprof
constraint(
    name = "sysprof",
    values = ["enabled", "disabled", "auto"],
    default = "auto",
    visibility = ["PUBLIC"],
)
