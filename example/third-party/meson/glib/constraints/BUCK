# Generated by decay. Do not edit.
#
# The options glib declares, defaulting to what meson itself would have
# chosen. Set them on a platform to configure this project. Options
# meson provides to every project live alongside, one directory up.

# build option `option:glib_debug`: Enable GLib debug infrastructure (distros typically want this disabled in production; see docs/macros.md)
constraint(
    name = "glib_debug",
    values = ["enabled", "disabled", "auto"],
    default = "enabled",
    visibility = ["PUBLIC"],
)

# build option `option:glib_assert`: Enable GLib assertion (see docs/macros.md)
constraint(
    name = "glib_assert",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# build option `option:glib_checks`: Enable GLib checks such as API guards (see docs/macros.md)
constraint(
    name = "glib_checks",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# build option `option:bsymbolic_functions`: link with -Bsymbolic-functions if supported
constraint(
    name = "bsymbolic_functions",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# build option `option:force_posix_threads`: Also use posix threads in case the platform defaults to another implementation (on Windows for example)
constraint(
    name = "force_posix_threads",
    values = ["true", "false"],
    default = "false",
    visibility = ["PUBLIC"],
)

# build option `option:xattr`: build with xattr support
constraint(
    name = "xattr",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# build option `option:file_monitor_backend`: The name of the system API to use as a GFileMonitor backend
constraint(
    name = "file_monitor_backend",
    values = ["auto", "inotify", "kqueue", "libinotify_kqueue", "win32"],
    default = "auto",
    visibility = ["PUBLIC"],
)

# build option `option:libelf`: Enable support for listing and extracting from ELF resource files with gresource tool
constraint(
    name = "libelf",
    values = ["enabled", "disabled", "auto"],
    default = "auto",
    visibility = ["PUBLIC"],
)
