# Generated by decay. Do not edit.
#
# The options zlib declares, defaulting to what meson itself would have
# chosen. Set them on a platform to configure this project. Options
# meson provides to every project live alongside, one directory up.

# build option `option:solo`: Build a minimal zlib without file I/O API
constraint(
    name = "solo",
    values = ["true", "false"],
    default = "false",
    visibility = ["PUBLIC"],
)
