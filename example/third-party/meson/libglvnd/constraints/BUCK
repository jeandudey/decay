# Generated by decay. Do not edit.
#
# The options glvnd declares, defaulting to what meson itself would have
# chosen. Set them on a platform to configure this project. Options
# meson provides to every project live alongside, one directory up.

# build option `option:asm`: Use ASM when compiling.
constraint(
    name = "asm",
    values = ["enabled", "disabled", "auto"],
    default = "auto",
    visibility = ["PUBLIC"],
)

# build option `option:glx`: Support the GLX platform.
constraint(
    name = "glx",
    values = ["enabled", "disabled", "auto"],
    default = "auto",
    visibility = ["PUBLIC"],
)

# build option `option:tls`: Use Thread Local Storage (if supported).
constraint(
    name = "tls",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# build option `option:dispatch-tls`: Use TLS-based stubs in libGLdispatch (if supported).
constraint(
    name = "dispatch_tls",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# build option `option:entrypoint-patching`: Allow vendor libraries to patch OpenGL entrypoint functions.
constraint(
    name = "entrypoint_patching",
    values = ["enabled", "disabled", "auto"],
    default = "auto",
    visibility = ["PUBLIC"],
)

# build option `option:gles1`: Support OpenGL ES 1.x.
constraint(
    name = "gles1",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# build option `option:gles2`: Support OpenGL ES 2.x and 3.x.
constraint(
    name = "gles2",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# build option `option:egl`: Support the EGL platform.
constraint(
    name = "egl",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)
