# Generated by decay. Do not edit.
#
# The options pixman declares, defaulting to what meson itself would have
# chosen. Set them on a platform to configure this project. Options
# meson provides to every project live alongside, one directory up.

# build option `option:mmx`: Use X86 MMX intrinsic optimized paths
constraint(
    name = "mmx",
    values = ["enabled", "disabled", "auto"],
    default = "auto",
    visibility = ["PUBLIC"],
)

# build option `option:sse2`: Use X86 SSE2 intrinsic optimized paths
constraint(
    name = "sse2",
    values = ["enabled", "disabled", "auto"],
    default = "auto",
    visibility = ["PUBLIC"],
)

# build option `option:ssse3`: Use X86 SSSE3 intrinsic optimized paths
constraint(
    name = "ssse3",
    values = ["enabled", "disabled", "auto"],
    default = "auto",
    visibility = ["PUBLIC"],
)

# build option `option:gnu-inline-asm`: Use GNU style inline assembler
constraint(
    name = "gnu_inline_asm",
    values = ["enabled", "disabled", "auto"],
    default = "auto",
    visibility = ["PUBLIC"],
)

# build option `option:timers`: Enable TIMER_* macros
constraint(
    name = "timers",
    values = ["true", "false"],
    default = "false",
    visibility = ["PUBLIC"],
)

# build option `option:gnuplot`: Enable output of filters that can be piped to gnuplot
constraint(
    name = "gnuplot",
    values = ["true", "false"],
    default = "false",
    visibility = ["PUBLIC"],
)

# build option `option:demos`: Build demos
constraint(
    name = "demos",
    values = ["enabled", "disabled", "auto"],
    default = "auto",
    visibility = ["PUBLIC"],
)

# build option `option:tls`: Use compiler support for thread-local storage
constraint(
    name = "tls",
    values = ["enabled", "disabled", "auto"],
    default = "auto",
    visibility = ["PUBLIC"],
)

# build option `option:tests`: Build tests
constraint(
    name = "tests",
    values = ["enabled", "disabled", "auto"],
    default = "auto",
    visibility = ["PUBLIC"],
)
