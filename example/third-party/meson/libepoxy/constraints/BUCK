# Generated by decay. Do not edit.
#
# The options libepoxy declares, defaulting to what meson itself would have
# chosen. Set them on a platform to configure this project. Options
# meson provides to every project live alongside, one directory up.

# build option `option:glx`: Enable GLX support
constraint(
    name = "glx",
    values = ["auto", "yes", "no"],
    default = "auto",
    visibility = ["PUBLIC"],
)

# build option `option:egl`: Enable EGL support
constraint(
    name = "egl",
    values = ["auto", "yes", "no"],
    default = "auto",
    visibility = ["PUBLIC"],
)

# build option `option:x11`: Enable X11 support (GLX or EGL-X11)
constraint(
    name = "x11",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# build option `option:tests`: Build the test suite
constraint(
    name = "tests",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# build option `option:docs`: Enable generating the Epoxy API reference (depends on Doxygen)
constraint(
    name = "docs",
    values = ["true", "false"],
    default = "false",
    visibility = ["PUBLIC"],
)
