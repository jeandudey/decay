# Generated by decay. Do not edit.
#
# The options libffi declares, defaulting to what meson itself would have
# chosen. Set them on a platform to configure this project. Options
# meson provides to every project live alongside, one directory up.

# build option `option:pax_emutramp`: pax_emutramp
constraint(
    name = "pax_emutramp",
    values = ["true", "false"],
    default = "false",
    visibility = ["PUBLIC"],
)

# build option `option:ffi-debug`: ffi-debug
constraint(
    name = "ffi_debug",
    values = ["true", "false"],
    default = "false",
    visibility = ["PUBLIC"],
)

# build option `option:structs`: structs
constraint(
    name = "structs",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# build option `option:raw_api`: raw_api
constraint(
    name = "raw_api",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# build option `option:exe_static_tramp`: exe_static_tramp
constraint(
    name = "exe_static_tramp",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# build option `option:purify_safety`: purify_safety
constraint(
    name = "purify_safety",
    values = ["true", "false"],
    default = "false",
    visibility = ["PUBLIC"],
)

# build option `option:ffi-build-versioned`: ffi-build-versioned
constraint(
    name = "ffi_build_versioned",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)
