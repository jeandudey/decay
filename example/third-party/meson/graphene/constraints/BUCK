# Generated by decay. Do not edit.
#
# The options graphene declares, defaulting to what meson itself would have
# chosen. Set them on a platform to configure this project. Options
# meson provides to every project live alongside, one directory up.

# build option `option:gobject_types`: Enable GObject types (depends on GObject)
constraint(
    name = "gobject_types",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# build option `option:sse2`: Enable SSE2 fast paths (requires SSE2 or later)
constraint(
    name = "sse2",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# build option `option:gcc_vector`: Enable GCC vector fast paths (requires GCC)
constraint(
    name = "gcc_vector",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# build option `option:arm_neon`: Enable ARM NEON fast paths (requires ARM)
constraint(
    name = "arm_neon",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# build option `option:tests`: Build the test suite (requires GObject)
constraint(
    name = "tests",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)
