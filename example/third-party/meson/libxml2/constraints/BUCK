# Generated by decay. Do not edit.
#
# The options libxml2 declares, defaulting to what meson itself would have
# chosen. Set them on a platform to configure this project. Options
# meson provides to every project live alongside, one directory up.

# build option `option:http`: ABI compatibility for removed HTTP support
constraint(
    name = "http",
    values = ["enabled", "disabled", "auto"],
    default = "auto",
    visibility = ["PUBLIC"],
)

# build option `option:icu`: ICU support
constraint(
    name = "icu",
    values = ["enabled", "disabled", "auto"],
    default = "disabled",
    visibility = ["PUBLIC"],
)

# build option `option:legacy`: Maximum ABI compatibility
constraint(
    name = "legacy",
    values = ["enabled", "disabled", "auto"],
    default = "disabled",
    visibility = ["PUBLIC"],
)

# build option `option:thread-alloc`: per-thread malloc hooks
constraint(
    name = "thread_alloc",
    values = ["enabled", "disabled", "auto"],
    default = "disabled",
    visibility = ["PUBLIC"],
)

# build option `option:tls`: thread-local storage
constraint(
    name = "tls",
    values = ["enabled", "disabled", "auto"],
    default = "disabled",
    visibility = ["PUBLIC"],
)

# build option `option:minimum`: build a minimally sized library
constraint(
    name = "minimum",
    values = ["true", "false"],
    default = "false",
    visibility = ["PUBLIC"],
)

# build option `option:catalog`: XML Catalogs support
constraint(
    name = "catalog",
    values = ["enabled", "disabled", "auto"],
    default = "auto",
    visibility = ["PUBLIC"],
)

# build option `option:debugging`: Debugging module and shell
constraint(
    name = "debugging",
    values = ["enabled", "disabled", "auto"],
    default = "auto",
    visibility = ["PUBLIC"],
)

# build option `option:html`: HTML parser
constraint(
    name = "html",
    values = ["enabled", "disabled", "auto"],
    default = "auto",
    visibility = ["PUBLIC"],
)

# build option `option:iconv`: iconv support
constraint(
    name = "iconv",
    values = ["enabled", "disabled", "auto"],
    default = "auto",
    visibility = ["PUBLIC"],
)

# build option `option:iso8859x`: ISO-8859-X support if no iconv
constraint(
    name = "iso8859x",
    values = ["enabled", "disabled", "auto"],
    default = "auto",
    visibility = ["PUBLIC"],
)

# build option `option:modules`: Dynamic modules support
constraint(
    name = "modules",
    values = ["enabled", "disabled", "auto"],
    default = "auto",
    visibility = ["PUBLIC"],
)

# build option `option:sax1`: Older SAX1 interface
constraint(
    name = "sax1",
    values = ["enabled", "disabled", "auto"],
    default = "auto",
    visibility = ["PUBLIC"],
)

# build option `option:threads`: Multithreading support
constraint(
    name = "threads",
    values = ["enabled", "disabled", "auto"],
    default = "auto",
    visibility = ["PUBLIC"],
)

# build option `option:valid`: DTD validation support
constraint(
    name = "valid",
    values = ["enabled", "disabled", "auto"],
    default = "auto",
    visibility = ["PUBLIC"],
)

# build option `option:xinclude`: XInclude 1.0 support
constraint(
    name = "xinclude",
    values = ["enabled", "disabled", "auto"],
    default = "auto",
    visibility = ["PUBLIC"],
)

# build option `option:zlib`: ZLIB support
constraint(
    name = "zlib",
    values = ["enabled", "disabled", "auto"],
    default = "auto",
    visibility = ["PUBLIC"],
)

# build option `option:output`: Serialization support
constraint(
    name = "output",
    values = ["enabled", "disabled", "auto"],
    default = "auto",
    visibility = ["PUBLIC"],
)

# build option `option:c14n`: Canonical XML 1.0 support
constraint(
    name = "c14n",
    values = ["enabled", "disabled", "auto"],
    default = "auto",
    visibility = ["PUBLIC"],
)

# build option `option:writer`: xmlWriter serialization interface
constraint(
    name = "writer",
    values = ["enabled", "disabled", "auto"],
    default = "auto",
    visibility = ["PUBLIC"],
)

# build option `option:pattern`: xmlPattern selection interface
constraint(
    name = "pattern",
    values = ["enabled", "disabled", "auto"],
    default = "auto",
    visibility = ["PUBLIC"],
)

# build option `option:schemas`: XML Schemas 1.0 support
constraint(
    name = "schemas",
    values = ["enabled", "disabled", "auto"],
    default = "auto",
    visibility = ["PUBLIC"],
)

# build option `option:schematron`: Schematron support
constraint(
    name = "schematron",
    values = ["enabled", "disabled", "auto"],
    default = "auto",
    visibility = ["PUBLIC"],
)

# build option `option:regexps`: Regular expressions support
constraint(
    name = "regexps",
    values = ["enabled", "disabled", "auto"],
    default = "auto",
    visibility = ["PUBLIC"],
)

# build option `option:relaxng`: RELAX NG support
constraint(
    name = "relaxng",
    values = ["enabled", "disabled", "auto"],
    default = "auto",
    visibility = ["PUBLIC"],
)

# build option `option:push`: push parser interfaces
constraint(
    name = "push",
    values = ["enabled", "disabled", "auto"],
    default = "auto",
    visibility = ["PUBLIC"],
)

# build option `option:reader`: xmlReader parsing interface
constraint(
    name = "reader",
    values = ["enabled", "disabled", "auto"],
    default = "auto",
    visibility = ["PUBLIC"],
)

# build option `option:readline`: use readline for shell history
constraint(
    name = "readline",
    values = ["enabled", "disabled", "auto"],
    default = "auto",
    visibility = ["PUBLIC"],
)

# build option `option:history`: History support for shell
constraint(
    name = "history",
    values = ["enabled", "disabled", "auto"],
    default = "auto",
    visibility = ["PUBLIC"],
)

# build option `option:xpath`: XPath 1.0 support
constraint(
    name = "xpath",
    values = ["enabled", "disabled", "auto"],
    default = "auto",
    visibility = ["PUBLIC"],
)

# build option `option:xptr`: XPointer support
constraint(
    name = "xptr",
    values = ["enabled", "disabled", "auto"],
    default = "auto",
    visibility = ["PUBLIC"],
)
