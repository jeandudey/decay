# Generated by decay. Do not edit.
#
# Constraints that come from meson itself rather than from any one
# project: meson's own build options, the toolchain, the machine
# being built for, and what the build environment provides. Every
# imported project selects on these, so they are declared once.

# A value no platform sets. A target that requires it exists in no
# configuration, which is how a conditional target is expressed.
constraint(
    name = "unsatisfiable",
    values = ["yes", "no"],
    default = "no",
    visibility = ["PUBLIC"],
)

# machine property `compiler:c`: the c compiler in use
constraint(
    name = "compiler",
    values = ["gcc", "clang"],
    default = "gcc",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:run:IEEE 754 floating-point division`: `IEEE 754 floating-point division` is available to the c compiler
constraint(
    name = "run_IEEE_754_floating_point_division",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:run:-mtls-dialect=gnu2`: `-mtls-dialect=gnu2` is available to the c compiler
constraint(
    name = "run_mtls_dialect_gnu2",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# machine property `compiler:cpp`: the cpp compiler in use
constraint(
    name = "cpp_compiler",
    values = ["gcc", "clang"],
    default = "gcc",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:run:C99 vsnprintf`: `C99 vsnprintf` is available to the c compiler
constraint(
    name = "run_C99_vsnprintf",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:run:C99 snprintf`: `C99 snprintf` is available to the c compiler
constraint(
    name = "run_C99_snprintf",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:run:Unix98 printf positional parameters`: `Unix98 printf positional parameters` is available to the c compiler
constraint(
    name = "run_Unix98_printf_positional_parameters",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# machine property `machine:host:endian`: host machine endian
constraint(
    name = "endian",
    values = ["little", "big"],
    default = "little",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:run:va_lists can be copied as values`: `va_lists can be copied as values` is available to the c compiler
constraint(
    name = "run_va_lists_can_be_copied_as_values",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:run:stack grows check`: `stack grows check` is available to the c compiler
constraint(
    name = "run_stack_grows_check",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:run:OpenBSD strlcpy/strlcat`: `OpenBSD strlcpy/strlcat` is available to the c compiler
constraint(
    name = "run_OpenBSD_strlcpy_strlcat",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:run:/proc/self/cmdline`: `/proc/self/cmdline` is available to the c compiler
constraint(
    name = "run_proc_self_cmdline",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:run:printf supports large precisions`: `printf supports large precisions` is available to the c compiler
constraint(
    name = "run_printf_supports_large_precisions",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:run:printf survives out-of-memory conditions`: `printf survives out-of-memory conditions` is available to the c compiler
constraint(
    name = "run_printf_survives_out_of_memory_conditions",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:run:printf supports the zero flag correctly`: `printf supports the zero flag correctly` is available to the c compiler
constraint(
    name = "run_printf_supports_the_zero_flag_correctly",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:run:printf supports the left-adjust flag correctly`: `printf supports the left-adjust flag correctly` is available to the c compiler
constraint(
    name = "run_printf_supports_the_left_adjust_flag_correctly",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:run:printf supports the grouping flag`: `printf supports the grouping flag` is available to the c compiler
constraint(
    name = "run_printf_supports_the_grouping_flag",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:run:printf supports the 'a' and 'A' directives`: `printf supports the 'a' and 'A' directives` is available to the c compiler
constraint(
    name = "run_printf_supports_the_a_and_A_directives",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:run:printf supports the 'F' directive`: `printf supports the 'F' directive` is available to the c compiler
constraint(
    name = "run_printf_supports_the_F_directive",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:run:printf supports the 'ls' directive`: `printf supports the 'ls' directive` is available to the c compiler
constraint(
    name = "run_printf_supports_the_ls_directive",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:run:printf supports 'long double' arguments`: `printf supports 'long double' arguments` is available to the c compiler
constraint(
    name = "run_printf_supports_long_double_arguments",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:run:printf supports infinite 'double' arguments`: `printf supports infinite 'double' arguments` is available to the c compiler
constraint(
    name = "run_printf_supports_infinite_double_arguments",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:run:printf supports infinite 'long double' arguments`: `printf supports infinite 'long double' arguments` is available to the c compiler
constraint(
    name = "run_printf_supports_infinite_long_double_arguments",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:run:ldexpl works`: `ldexpl works` is available to the c compiler
constraint(
    name = "run_ldexpl_works",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:run:dlsym() preceding underscores`: `dlsym() preceding underscores` is available to the c compiler
constraint(
    name = "run_dlsym_preceding_underscores",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# meson build option `option:debug`
constraint(
    name = "debug",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)
