# Generated by decay. Do not edit.
#
# Constraints that come from meson itself rather than from any one
# project: meson's own build options, the toolchain, the machine
# being built for, and what the build environment provides. Every
# imported project selects on these, so they are declared once.

# A value no platform sets. A target that requires it exists in no
# configuration, which is how a conditional target is expressed.
constraint(
    name = "unsatisfiable",
    values = ["yes", "no"],
    default = "no",
    visibility = ["PUBLIC"],
)

# machine property `compiler:c`: the c compiler in use
constraint(
    name = "compiler",
    values = ["gcc", "clang", "msvc"],
    default = "gcc",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:has_header:pthread.h`: `pthread.h` is available to the c compiler
constraint(
    name = "has_header_pthread_h",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:run:IEEE 754 floating-point division`: `IEEE 754 floating-point division` is available to the c compiler
constraint(
    name = "run_IEEE_754_floating_point_division",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:has_header_symbol:features.h`: `features.h` is available to the c compiler
constraint(
    name = "has_header_symbol_features_h",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:run:-mtls-dialect=gnu2`: `-mtls-dialect=gnu2` is available to the c compiler
constraint(
    name = "run_mtls_dialect_gnu2",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:has_header_symbol:dlfcn.h`: `dlfcn.h` is available to the c compiler
constraint(
    name = "has_header_symbol_dlfcn_h",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:has_member:struct dirent`: `struct dirent` is available to the c compiler
constraint(
    name = "has_member_struct_dirent",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# machine property `compiler:cpp`: the cpp compiler in use
constraint(
    name = "cpp_compiler",
    values = ["gcc", "clang", "msvc"],
    default = "gcc",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:check_header:malloc.h`: `malloc.h` is available to the c compiler
constraint(
    name = "check_header_malloc_h",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:has_header_symbol:locale.h`: `locale.h` is available to the c compiler
constraint(
    name = "has_header_symbol_locale_h",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:compiles:type "struct stat" has member "st_mtim.tv_nsec"`: `type "struct stat" has member "st_mtim.tv_nsec"` is available to the c compiler
constraint(
    name = "compiles_type_struct_stat_has_member_st_mtim_tv_nsec",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:compiles:type "struct stat" has member "st_atim.tv_nsec"`: `type "struct stat" has member "st_atim.tv_nsec"` is available to the c compiler
constraint(
    name = "compiles_type_struct_stat_has_member_st_atim_tv_nsec",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:compiles:type "struct stat" has member "st_ctim.tv_nsec"`: `type "struct stat" has member "st_ctim.tv_nsec"` is available to the c compiler
constraint(
    name = "compiles_type_struct_stat_has_member_st_ctim_tv_nsec",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:compiles:type "struct stat" has member "st_blksize"`: `type "struct stat" has member "st_blksize"` is available to the c compiler
constraint(
    name = "compiles_type_struct_stat_has_member_st_blksize",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:compiles:type "struct stat" has member "st_blocks"`: `type "struct stat" has member "st_blocks"` is available to the c compiler
constraint(
    name = "compiles_type_struct_stat_has_member_st_blocks",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:compiles:type "struct statfs" has member "f_bavail"`: `type "struct statfs" has member "f_bavail"` is available to the c compiler
constraint(
    name = "compiles_type_struct_statfs_has_member_f_bavail",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:compiles:type "struct dirent" has member "d_type"`: `type "struct dirent" has member "d_type"` is available to the c compiler
constraint(
    name = "compiles_type_struct_dirent_has_member_d_type",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:compiles:type "struct statvfs" has member "f_type"`: `type "struct statvfs" has member "f_type"` is available to the c compiler
constraint(
    name = "compiles_type_struct_statvfs_has_member_f_type",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:compiles:type "struct tm" has member "tm_gmtoff"`: `type "struct tm" has member "tm_gmtoff"` is available to the c compiler
constraint(
    name = "compiles_type_struct_tm_has_member_tm_gmtoff",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:has_header_symbol:sys/sysmacros.h`: `sys/sysmacros.h` is available to the c compiler
constraint(
    name = "has_header_symbol_sys_sysmacros_h",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:has_header_symbol:sys/mkdev.h`: `sys/mkdev.h` is available to the c compiler
constraint(
    name = "has_header_symbol_sys_mkdev_h",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:has_header_symbol:sys/types.h`: `sys/types.h` is available to the c compiler
constraint(
    name = "has_header_symbol_sys_types_h",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:links:eventfd(2) system call`: `eventfd(2) system call` is available to the c compiler
constraint(
    name = "links_eventfd_2_system_call",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:links:ppoll(2) system call`: `ppoll(2) system call` is available to the c compiler
constraint(
    name = "links_ppoll_2_system_call",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:links:pidfd_open(2) system call`: `pidfd_open(2) system call` is available to the c compiler
constraint(
    name = "links_pidfd_open_2_system_call",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:links:clock_gettime`: `clock_gettime` is available to the c compiler
constraint(
    name = "links_clock_gettime",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:links:clock_gettime in librt`: `clock_gettime in librt` is available to the c compiler
constraint(
    name = "links_clock_gettime_in_librt",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:links:dlopen() and dlsym() in system libraries`: `dlopen() and dlsym() in system libraries` is available to the c compiler
constraint(
    name = "links_dlopen_and_dlsym_in_system_libraries",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:links:dlopen() and dlsym() in libdl`: `dlopen() and dlsym() in libdl` is available to the c compiler
constraint(
    name = "links_dlopen_and_dlsym_in_libdl",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:compiles:number of arguments to statfs() (n=2)`: `number of arguments to statfs() (n=2)` is available to the c compiler
constraint(
    name = "compiles_number_of_arguments_to_statfs_n_2",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:compiles:number of arguments to statfs() (n=4)`: `number of arguments to statfs() (n=4)` is available to the c compiler
constraint(
    name = "compiles_number_of_arguments_to_statfs_n_4",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:run:C99 vsnprintf`: `C99 vsnprintf` is available to the c compiler
constraint(
    name = "run_C99_vsnprintf",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:run:C99 snprintf`: `C99 snprintf` is available to the c compiler
constraint(
    name = "run_C99_snprintf",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:run:Unix98 printf positional parameters`: `Unix98 printf positional parameters` is available to the c compiler
constraint(
    name = "run_Unix98_printf_positional_parameters",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:links:nl_langinfo and CODESET`: `nl_langinfo and CODESET` is available to the c compiler
constraint(
    name = "links_nl_langinfo_and_CODESET",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:links:nl_langinfo (PM_STR)`: `nl_langinfo (PM_STR)` is available to the c compiler
constraint(
    name = "links_nl_langinfo_PM_STR",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:links:nl_langinfo (ERA)`: `nl_langinfo (ERA)` is available to the c compiler
constraint(
    name = "links_nl_langinfo_ERA",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:links:nl_langinfo (_NL_CTYPE_OUTDIGITn_MB)`: `nl_langinfo (_NL_CTYPE_OUTDIGITn_MB)` is available to the c compiler
constraint(
    name = "links_nl_langinfo_NL_CTYPE_OUTDIGITn_MB",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:links:nl_langinfo (ALTMON_n)`: `nl_langinfo (ALTMON_n)` is available to the c compiler
constraint(
    name = "links_nl_langinfo_ALTMON_n",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:links:nl_langinfo (_NL_ABALTMON_n)`: `nl_langinfo (_NL_ABALTMON_n)` is available to the c compiler
constraint(
    name = "links_nl_langinfo_NL_ABALTMON_n",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:links:nl_langinfo and _NL_TIME_CODESET`: `nl_langinfo and _NL_TIME_CODESET` is available to the c compiler
constraint(
    name = "links_nl_langinfo_and_NL_TIME_CODESET",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:has_header_symbol:stddef.h`: `stddef.h` is available to the c compiler
constraint(
    name = "has_header_symbol_stddef_h",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:links:sig_atomic_t`: `sig_atomic_t` is available to the c compiler
constraint(
    name = "links_sig_atomic_t",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:has_header_symbol:wchar.h`: `wchar.h` is available to the c compiler
constraint(
    name = "has_header_symbol_wchar_h",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# machine property `machine:host:endian`: host machine endian
constraint(
    name = "endian",
    values = ["little", "big"],
    default = "little",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:run:va_lists can be copied as values`: `va_lists can be copied as values` is available to the c compiler
constraint(
    name = "run_va_lists_can_be_copied_as_values",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:links:atomic ops`: `atomic ops` is available to the c compiler
constraint(
    name = "links_atomic_ops",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:links:sync swap`: `sync swap` is available to the c compiler
constraint(
    name = "links_sync_swap",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:has_header_symbol:pthread.h`: `pthread.h` is available to the c compiler
constraint(
    name = "has_header_symbol_pthread_h",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:links:pthread_setname_np(pthread_t, const char*)`: `pthread_setname_np(pthread_t, const char*)` is available to the c compiler
constraint(
    name = "links_pthread_setname_np_pthread_t_const_char",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:links:pthread_setname_np(pthread_t, const char*, void*)`: `pthread_setname_np(pthread_t, const char*, void*)` is available to the c compiler
constraint(
    name = "links_pthread_setname_np_pthread_t_const_char_void",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:links:pthread_set_name_np(pthread_t, const char*)`: `pthread_set_name_np(pthread_t, const char*)` is available to the c compiler
constraint(
    name = "links_pthread_set_name_np_pthread_t_const_char",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:run:stack grows check`: `stack grows check` is available to the c compiler
constraint(
    name = "run_stack_grows_check",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:links:check for -latomic`: `check for -latomic` is available to the c compiler
constraint(
    name = "links_check_for_latomic",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# external dependency `dep:mount`: `mount` is available
constraint(
    name = "mount",
    values = ["true", "false"],
    default = "false",
    visibility = ["PUBLIC"],
)

# external dependency `dep:libselinux`: `libselinux` is available
constraint(
    name = "libselinux",
    values = ["true", "false"],
    default = "false",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:run:OpenBSD strlcpy/strlcat`: `OpenBSD strlcpy/strlcat` is available to the c compiler
constraint(
    name = "run_OpenBSD_strlcpy_strlcat",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:run:/proc/self/cmdline`: `/proc/self/cmdline` is available to the c compiler
constraint(
    name = "run_proc_self_cmdline",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:has_header_symbol:sys/ptrace.h`: `sys/ptrace.h` is available to the c compiler
constraint(
    name = "has_header_symbol_sys_ptrace_h",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# external dependency `dep:sysprof-capture-4`: `sysprof-capture-4` is available
constraint(
    name = "sysprof_capture_4",
    values = ["true", "false"],
    default = "false",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:run:printf supports large precisions`: `printf supports large precisions` is available to the c compiler
constraint(
    name = "run_printf_supports_large_precisions",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:run:printf survives out-of-memory conditions`: `printf survives out-of-memory conditions` is available to the c compiler
constraint(
    name = "run_printf_survives_out_of_memory_conditions",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:run:printf supports the zero flag correctly`: `printf supports the zero flag correctly` is available to the c compiler
constraint(
    name = "run_printf_supports_the_zero_flag_correctly",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:run:printf supports the left-adjust flag correctly`: `printf supports the left-adjust flag correctly` is available to the c compiler
constraint(
    name = "run_printf_supports_the_left_adjust_flag_correctly",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:run:printf supports the grouping flag`: `printf supports the grouping flag` is available to the c compiler
constraint(
    name = "run_printf_supports_the_grouping_flag",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:run:printf supports the 'a' and 'A' directives`: `printf supports the 'a' and 'A' directives` is available to the c compiler
constraint(
    name = "run_printf_supports_the_a_and_A_directives",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:run:printf supports the 'F' directive`: `printf supports the 'F' directive` is available to the c compiler
constraint(
    name = "run_printf_supports_the_F_directive",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:run:printf supports the 'ls' directive`: `printf supports the 'ls' directive` is available to the c compiler
constraint(
    name = "run_printf_supports_the_ls_directive",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:run:printf supports 'long double' arguments`: `printf supports 'long double' arguments` is available to the c compiler
constraint(
    name = "run_printf_supports_long_double_arguments",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:run:printf supports infinite 'double' arguments`: `printf supports infinite 'double' arguments` is available to the c compiler
constraint(
    name = "run_printf_supports_infinite_double_arguments",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:run:printf supports infinite 'long double' arguments`: `printf supports infinite 'long double' arguments` is available to the c compiler
constraint(
    name = "run_printf_supports_infinite_long_double_arguments",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:links:#include <math.h>
#               #if __GNUC__ >= 4
#               # undef isnand
#               # define isnand(x) __builtin_isnan ((double)(x))
#               #else
#               # undef isnand
#               # define isnand(x) isnan ((double)(x))
#               #endif
#               double x;
#               int main () {return isnand (x);}
#            `: `#include <math.h>
#               #if __GNUC__ >= 4
#               # undef isnand
#               # define isnand(x) __builtin_isnan ((double)(x))
#               #else
#               # undef isnand
#               # define isnand(x) isnan ((double)(x))
#               #endif
#               double x;
#               int main () {return isnand (x);}
#            ` is available to the c compiler
constraint(
    name = "links_include_math_h_if_GNUC_4_undef_isnand_define_isnand_x_builtin_isnan_double_x_else_undef_isnand_define_isnand_x_isnan_double_x_endif_double_x_int_main_return_isnand_x",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:links:#include <math.h>
#               #if __GNUC__ >= 4
#               # undef isnanf
#               # define isnanf(x) __builtin_isnanf ((float)(x))
#               #else
#               # undef isnanf
#               # define isnanf(x) isnan ((float)(x))
#               #endif
#               double x;
#               int main () {return isnanf (x);}
#            `: `#include <math.h>
#               #if __GNUC__ >= 4
#               # undef isnanf
#               # define isnanf(x) __builtin_isnanf ((float)(x))
#               #else
#               # undef isnanf
#               # define isnanf(x) isnan ((float)(x))
#               #endif
#               double x;
#               int main () {return isnanf (x);}
#            ` is available to the c compiler
constraint(
    name = "links_include_math_h_if_GNUC_4_undef_isnanf_define_isnanf_x_builtin_isnanf_float_x_else_undef_isnanf_define_isnanf_x_isnan_float_x_endif_double_x_int_main_return_isnanf_x",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:links:#include <math.h>
#               #if __GNUC__ >= 4
#               # undef isnanl
#               # define isnanl(x) __builtin_isnanl ((long double)(x))
#               #else
#               # undef isnanl
#               # define isnanl(x) isnan ((long double)(x))
#               #endif
#               double x;
#               int main () {return isnanl (x);}
#            `: `#include <math.h>
#               #if __GNUC__ >= 4
#               # undef isnanl
#               # define isnanl(x) __builtin_isnanl ((long double)(x))
#               #else
#               # undef isnanl
#               # define isnanl(x) isnan ((long double)(x))
#               #endif
#               double x;
#               int main () {return isnanl (x);}
#            ` is available to the c compiler
constraint(
    name = "links_include_math_h_if_GNUC_4_undef_isnanl_define_isnanl_x_builtin_isnanl_long_double_x_else_undef_isnanl_define_isnanl_x_isnan_long_double_x_endif_double_x_int_main_return_isnanl_x",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:links:#include <math.h>
#                      double x;
#                      int y;
#                      int main () {return ldexp (x, y) < 1;}
#                   `: `#include <math.h>
#                      double x;
#                      int y;
#                      int main () {return ldexp (x, y) < 1;}
#                   ` is available to the c compiler
constraint(
    name = "links_include_math_h_double_x_int_y_int_main_return_ldexp_x_y_1",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:links:#include <math.h>
#                      long double x;
#                      int main () {return ldexpl (x, -1) > 0;}
#                   `: `#include <math.h>
#                      long double x;
#                      int main () {return ldexpl (x, -1) > 0;}
#                   ` is available to the c compiler
constraint(
    name = "links_include_math_h_long_double_x_int_main_return_ldexpl_x_1_0",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:links:#include <math.h>
#                      long double x;
#                      int main () {int e; return frexpl (x, &e) > 0;}
#                   `: `#include <math.h>
#                      long double x;
#                      int main () {int e; return frexpl (x, &e) > 0;}
#                   ` is available to the c compiler
constraint(
    name = "links_include_math_h_long_double_x_int_main_int_e_return_frexpl_x_e_0",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:run:ldexpl works`: `ldexpl works` is available to the c compiler
constraint(
    name = "run_ldexpl_works",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:links:#include <math.h>
#               double x;
#               int main () {return isfinite (x);}
#            `: `#include <math.h>
#               double x;
#               int main () {return isfinite (x);}
#            ` is available to the c compiler
constraint(
    name = "links_include_math_h_double_x_int_main_return_isfinite_x",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:links:#include <math.h>
#               double x;
#               int main () {return isinf (x);}
#            `: `#include <math.h>
#               double x;
#               int main () {return isinf (x);}
#            ` is available to the c compiler
constraint(
    name = "links_include_math_h_double_x_int_main_return_isinf_x",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:links:#include <math.h>
#               double x;
#               int main () {return signbit (x);}
#            `: `#include <math.h>
#               double x;
#               int main () {return signbit (x);}
#            ` is available to the c compiler
constraint(
    name = "links_include_math_h_double_x_int_main_return_signbit_x",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:run:dlsym() preceding underscores`: `dlsym() preceding underscores` is available to the c compiler
constraint(
    name = "run_dlsym_preceding_underscores",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:links:res_query()`: `res_query()` is available to the c compiler
constraint(
    name = "links_res_query",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:links:res_query() in -lresolv`: `res_query() in -lresolv` is available to the c compiler
constraint(
    name = "links_res_query_in_lresolv",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:links:res_query() in -lbind`: `res_query() in -lbind` is available to the c compiler
constraint(
    name = "links_res_query_in_lbind",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# external dependency `lib:bind`: `bind` is available
constraint(
    name = "bind",
    values = ["true", "false"],
    default = "false",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:links:res_query() in -lsocket`: `res_query() in -lsocket` is available to the c compiler
constraint(
    name = "links_res_query_in_lsocket",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# external dependency `lib:socket`: `socket` is available
constraint(
    name = "socket",
    values = ["true", "false"],
    default = "false",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:links:socket()`: `socket()` is available to the c compiler
constraint(
    name = "links_socket",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:links:socket() in -lsocket`: `socket() in -lsocket` is available to the c compiler
constraint(
    name = "links_socket_in_lsocket",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:links:dn_comp()`: `dn_comp()` is available to the c compiler
constraint(
    name = "links_dn_comp",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:links:res_nclose()`: `res_nclose()` is available to the c compiler
constraint(
    name = "links_res_nclose",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:links:res_ninit()`: `res_ninit()` is available to the c compiler
constraint(
    name = "links_res_ninit",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:links:res_nquery()`: `res_nquery()` is available to the c compiler
constraint(
    name = "links_res_nquery",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# external dependency `dep:libinotify`: `libinotify` is available
constraint(
    name = "libinotify",
    values = ["true", "false"],
    default = "false",
    visibility = ["PUBLIC"],
)

# external dependency `dep:libelf`: `libelf` is available
constraint(
    name = "libelf",
    values = ["true", "false"],
    default = "false",
    visibility = ["PUBLIC"],
)

# external dependency `lib:FuzzingEngine`: `FuzzingEngine` is available
constraint(
    name = "FuzzingEngine",
    values = ["true", "false"],
    default = "false",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:links:__builtin_unreachable`: `__builtin_unreachable` is available to the c compiler
constraint(
    name = "links_builtin_unreachable",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:links:__builtin_mul_overflow`: `__builtin_mul_overflow` is available to the c compiler
constraint(
    name = "links_builtin_mul_overflow",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)

# toolchain probe `probe:c:links:__assume`: `__assume` is available to the c compiler
constraint(
    name = "links_assume",
    values = ["true", "false"],
    default = "true",
    visibility = ["PUBLIC"],
)
